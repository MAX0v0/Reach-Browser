# Reach-Browser 配置模板
# 复制为 .env 并填入真实值（.env 已被 .gitignore 排除，不会提交）

# Anthropic API Key
ANTHROPIC_API_KEY=sk-your-key-here

# 自定义 Base URL（代理中转节点；留空则使用官方 https://api.anthropic.com）
CUSTOM_BASE_URL=

# 网络代理（可选，格式：http://user:pass@host:port）
HTTP_PROXY=
HTTPS_PROXY=
